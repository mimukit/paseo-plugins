> Copy the block below into `<plugin-dir>/README.md` and fill it in. Do not copy this line or the stamp at the bottom of this page.

---

# <plugin-name>

One sentence on what it does.

## What it adds

- Command center: `<title>` (context: workspace)
- Composer pill: `<label>`
- Workspace panel: `<title>`

## Install

```sh
paseo plugin install mimukit/paseo-plugins --path <plugin-dir>
```

## Requirements

- Paseo daemon running
- `git` and `gh` on PATH, `gh` authenticated (drop this line if the plugin shells out to nothing)

## Configuration

State the manifest keys or environment variables it reads. Write "None." when it reads none.

## Notes

Anything a future reader needs: known limits, what the plugin refuses to do, which skill owns the rule it displays.

_Verified against `main`@`c4b0a58` on 2026-09-01._
